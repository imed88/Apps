@model IEnumerable<Apps.Models.Tables.Specialites>

@{
    ViewData["Title"] = "Index";
}
<link href="~/css/CustomStyle.css" rel="stylesheet" />
<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
   
    
@*foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nameSpecialite)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.idSpecialite">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.idSpecialite">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.idSpecialite">Delete</a>
            </td>
        </tr>
}
    
*@
<div class="container">
    <div class="row">
        @using (Html.BeginForm("Index", "Specialites", FormMethod.Get))
        {
            @Html.TextBox("searching");
            <input type="submit" value="submit" />
        }
    </div>
    <div class="row">
    <div class="row justify-content-center m-3">
        <div class="card-deck">
            @foreach (var item in Model)
            {
                <div class="col-md-4">
                    <div class="card card-block " style="background-color:black; color:white; width: 25rem; margin:5rem; display: inline-block ">
                        <div class="card-header ">

                        </div>
                        <div class="card-body text-center">
                            @Html.DisplayFor(modelItem => item.nameSpecialite)

                        </div>

                    </div>
                </div>
            }

        </div>
    </div>
</div>
    </div>



